] add speed 0.05
[ add speed -0.05
# Replaced: [, ]

, playlist-prev
. playlist-next
# Replaced: \., \,

< set pause yes; frame-back-step
> set pause yes; frame-step
# Replaced: <, >

n run file_manager_desktop "${path}"
# Claimed: n

LEFT no-osd seek -1 exact
# Replaced: LEFT

RIGHT no-osd seek 1 exact
# Replaced: RIGHT

MBTN_LEFT cycle pause
# Claimed: MBTN_LEFT

MBTN_MID cycle pause
# Claimed: MBTN_MID

WHEEL_LEFT no-osd seek -0.5 exact
# Replaced: WHEEL_LEFT

WHEEL_RIGHT no-osd seek 0.5 exact
# Replaced: WHEEL_RIGHT

WHEEL_UP add speed 0.05
# Replaced: WHEEL_UP

WHEEL_DOWN add speed -0.05
# Replaced: WHEEL_DOWN

Shift+WHEEL_UP add volume 2
# Claimed: Shift+WHEEL_UP

Shift+WHEEL_DOWN add volume -2
# Claimed: Shift+WHEEL_DOWN
