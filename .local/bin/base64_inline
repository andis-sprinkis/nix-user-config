#!/usr/bin/env sh
set -euf
IFS_="$IFS"
NL="
"

if [ "${1:-""}" ]; then
  printf '%s' "$1" | base64 -w0
  exit
fi

text=""
IFS=""
while read -r l; do
  IFS="$IFS_"

  text="${text}${text:+"$NL"}${l}"
done
IFS="$IFS_"

if [ "$text" ]; then
  printf '%s' "$text" | base64 -w0
fi
