#!/usr/bin/env sh
set -euf

fpath="$(readlink -f "$1")"
fname="$(basename "$fpath")"

ffmpeg -i "$fpath" -vcodec "libx264" -crf "23" -preset "medium" -acodec "aac" -movflags "+faststart" "${fname%.mov}_output.mp4"
