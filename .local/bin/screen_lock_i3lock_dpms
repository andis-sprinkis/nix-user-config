#!/usr/bin/env bash

revert() { xset dpms 0 0 0; }

color="$(xrdb -get background)"
[ -z "$color" ] && color="#000000"

trap revert HUP INT TERM
xset +dpms dpms 5 5 5
i3lock -u -n -c ${color:1}
revert
