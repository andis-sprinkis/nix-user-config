#!/usr/bin/env sh
#
[ "$DISPLAY" ] && [ -z "$WAYLAND_DISPLAY" ] && {
  killall -e -s 9 "xss-lock"
  killall -e -s 9 "xscreensaver"
  killall -e -s 9 "xscreensaver-wayland"
  exit
}

[ "$WAYLAND_DISPLAY" ] && {
  killall -e -s 9 "swayidle"
}
