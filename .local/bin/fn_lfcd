#!/bin/sh

lfcd() {
  tmp="$(mktemp)"
  if [ -f "$tmp" ]; then
    lf -last-dir-path="$tmp" "$@"
    dir="$(cat "$tmp")"
    rm -f "$tmp"
    [ -d "$dir" ] && [ "$dir" != "$(pwd)" ] && cd "$dir"
  fi
}
