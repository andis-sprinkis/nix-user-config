#!/usr/bin/env sh
set -euf

# shellcheck disable=SC3028
case "${OSTYPE:-"$(uname)"}" in
  [dD]"arwin"*)
    open -a "QuickTime" "$@"
  ;;
  *)
    exec mpv --player-operation-mode=pseudo-gui -- "$@"
  ;;
esac

exit "1"
