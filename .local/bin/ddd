#!/usr/bin/env sh
set -eu

if [ "${WAYLAND_DISPLAY:-""}" ]; then
  exec swayscale "1" "/usr/bin/ddd" "$@"
fi

if [ "${DISPLAY:-""}" ] && [ -z "${WAYLAND_DISPLAY:-""}" ]; then
  exec "/usr/bin/ddd" "$@"
fi
