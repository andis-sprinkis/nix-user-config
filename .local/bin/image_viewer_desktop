#!/usr/bin/env sh

# shellcheck disable=SC3028
case "${OSTYPE:-"$(uname)"}" in
  [dD]"arwin"*)
    open -a "Preview" "$@"
  ;;
  *)
    exec nsxiv-rifle "$@"

    # if [ "${WAYLAND_DISPLAY:-""}" ]; then
    #   exec swayimg "$@"
    # fi
    #
    # if [ "${DISPLAY:-""}" ]; then
    #   exec nsxiv-rifle "$@"
    # fi
  ;;
esac

exit "1"
