#!/usr/bin/env sh

chmod "700" "${HOME}/.ssh"
chmod "600" "${HOME}/.ssh/"*
chmod "644" "${HOME}/.ssh/"*".pub" "${HOME}/.ssh/authorized_keys" "${HOME}/.ssh/known_hosts"
chmod "700" "${HOME}/.ssh/agent"
