#!/usr/bin/env sh
foreground="$(xrdb -get foreground)"
color0="$(xrdb -get color0)"
font="$(xrdb -get font)"

exec /usr/bin/dmenu \
  -nf "$foreground" \
  -nb "$color0" \
  -sb "$foreground" \
  -sf "$color0" \
  -fn "$font" \
  -l "25" \
  -i \
  -b \
  "$@"
